#include <stdio.h>
#include <stdint.h>
#include <stdlib.h>
#include "doublelinkedlist.h"



int main ()
{

	printf("Unit Tests on Linked Lists");


/*	node* headref = NULL;

 		uint8_t ret;
 		uint32_t node_number = 0;
 		uint32_t data_removed = 0;
		ret = add_node(&headref, 0, 0);
        ret = add_node(&headref, 1, 1);
		ret = add_node(&headref, 2, 2);
		ret = add_node(&headref, 3, 3);
		ret = add_node(&headref, 4, 4);
		ret = add_node(&headref, 5, 5);
        print_list(&headref);
        ret = remove_node(&headref, 4, &data_removed);
        printf("\nRemoved Data = %d", data_removed);
        print_list(&headref);
        node* found = search_node(&headref, 5, &node_number);
       	printf("\nfound: %d", found->data);
       	printf("\nIndex of node: %d", node_number);
       	ret = destroy(&headref);
        printf("\ndestroy return: %d", ret);
        print_list(&headref);


*/
       	return 0;

}
